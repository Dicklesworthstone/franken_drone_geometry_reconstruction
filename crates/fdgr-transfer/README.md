# `fdgr-transfer` — Verified object-graph movement reference

**Status:** target crate contract; not yet a Cargo workspace member.

Defines manifests, closure verification, staging, resume journals, root-last receive publication, path races, repair material, and the rule that mutation authority never rides bulk transfer.

The first implementation must be simple and deterministic. Promotion into the workspace requires
the dependency-DAG, differential, cancellation, fault, schema, and agent-acceptance gates in the
comprehensive plan.
